What is JSX, and why is it used?
=It makes code easier to read and write, because you can directly describe how the UI should look.

2. What is the difference between State and Props?
=State is Managed within a component (local data) and manage time
Props is short for “properties." Data passed from parent to child component. Read-only (cannot be modified by the child).

3. What is the useState hook, and how does it work?
= useState is a React hook that add state to functional components.

4. How can you share state between components in React?
=The parent holds the state, and passes it down as props to child components.

5. How is event handling done in React?
=Instead of strings, you pass functions as event handlers.


